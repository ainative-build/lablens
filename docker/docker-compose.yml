services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - ../src:/app/src
    command: uvicorn lablens.main:app --host 0.0.0.0 --port 8000 --reload
